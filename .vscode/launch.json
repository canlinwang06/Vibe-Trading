{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Vibe-Trading Backend",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/.venv/bin/vibe-trading",
      "args": ["serve", "--port", "8899"],
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}",
      "envFile": "${workspaceFolder}/agent/.env"
    }
  ]
}
